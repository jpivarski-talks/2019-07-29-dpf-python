name: dpf-python
channels:
  - conda-forge
dependencies:
  - numpy<1.17.0        # technically fixed in awkward 0.12.4, but I don't want to rock the boat
  - uproot
  - pandas
  - matplotlib
  - iminuit
  - scikit-learn
  - numba
  - dask
  - python-graphviz
  - cython
  - pybind11
  - root
  - h5py
  - pyarrow
  - pip
  - pip:
      - pyjet
      - particle
      - pyhf
      - root_numpy
      - coffea==0.6.6   # requested by Nick
